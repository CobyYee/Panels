import { Typography, Box } from '@mui/material';

export default function PasswordResetScreen() {
    console.log("hi")
    return (
        <div id = 'home-wrapper'>
            <Typography sx = {{left: '10%', fontSize: '100px'}}> password reset screen </Typography>
        </div>
    )
}