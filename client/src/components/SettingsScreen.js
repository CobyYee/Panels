import { Typography, Box } from '@mui/material';

export default function SettingsScreen() {
    console.log("hi")
    return (
        <div id = 'settings-screen'>
            <Typography sx = {{left: '10%', fontSize: '100px'}}> SETTINGS SCREEN </Typography>
        </div>
    )
}