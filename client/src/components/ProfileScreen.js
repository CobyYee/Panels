import { Typography, Box } from '@mui/material';

export default function ProfileScreen() {
    console.log("hi")
    return (
        <div id = 'profile-screen'>
            <Typography sx = {{left: '10%', fontSize: '100px'}}> profile screen </Typography>
        </div>
    )
}