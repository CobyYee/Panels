import { Typography, Box } from '@mui/material';

export default function ListScreen() {
    return (
        <div id = 'home-wrapper'>
            <Typography sx = {{left: '10%', fontSize: '100px'}}> list screen </Typography>
        </div>
    )
}